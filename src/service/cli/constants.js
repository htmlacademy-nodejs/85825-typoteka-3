'use strict';

const DEFAULT_COUNT = 1;
const MAX_COUNT = 1000;
const MAX_COMMENTS = 4;
const ANNOUNCE_LENGTH = 5;
const MONTH_RESTRICT = 3;
const FILE_NAME = `mocks.json`;
const TEXT_FILE_PATH = `data`;
const DEFAULT_PORT = 3000;

module.exports = {
  DEFAULT_COUNT,
  MAX_COUNT,
  ANNOUNCE_LENGTH,
  MONTH_RESTRICT,
  FILE_NAME,
  TEXT_FILE_PATH,
  DEFAULT_PORT,
  MAX_COMMENTS
};
